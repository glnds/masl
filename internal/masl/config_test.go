package masl

import (
	"testing"
	// "fmt"
)

func TestGetConfig(t *testing.T) {
	// config := GetConfig("../../masl-example.toml", nil)
	// if config.BaseURL != "BaseURL" {
	// 	t.Error("ddd")
	// }
}
